services:
  app:
    build: .
    container_name: maintenance-app
    ports:
      - "3000:3000"
    volumes:
      - ./prisma/dev.db:/app/prisma/dev.db
      - ./public/uploads:/app/public/uploads
    environment:
      - DATABASE_URL=file:./dev.db
      - AUTH_SECRET=your_auth_secret_here # Change this in your .env
      - AUTH_TRUST_HOST=true
    restart: always
